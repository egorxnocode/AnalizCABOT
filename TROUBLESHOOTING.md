# 🛠️ Устранение неполадок Telegram Bot

## 🔍 Проблема: Pool timeout ошибки

### Симптомы
```
Pool timeout: All connections in the connection pool are occupied. 
Request was *not* sent to Telegram.
```

### Причины
- Недостаточный размер пула соединений
- Слишком короткие таймауты
- Высокая нагрузка на бота
- Медленные ответы от Telegram API

### ✅ Решение (уже реализовано)

#### 1. Увеличены настройки пула соединений в `.env`:
```env
CONNECTION_POOL_SIZE=25      # Размер пула соединений
POOL_TIMEOUT=45.0           # Таймаут получения соединения
READ_TIMEOUT=45.0           # Таймаут чтения
WRITE_TIMEOUT=45.0          # Таймаут записи
CONNECT_TIMEOUT=15.0        # Таймаут подключения
```

#### 2. Обновлен код бота:
- Добавлен HTTPXRequest с увеличенными лимитами
- Реализован retry механизм для запросов
- Улучшена обработка ошибок
- Добавлены безопасные методы отправки сообщений

#### 3. Обновлены зависимости:
- `python-telegram-bot[http2]==21.9` - последняя версия с исправлениями
- `httpx[http2]==0.27.2` - для лучшей производительности HTTP

## 🚀 Как применить исправления

### На сервере где запущен бот:

1. **Остановите бота:**
   ```bash
   ./scripts/stop.sh
   ```

2. **Обновите код:**
   ```bash
   git pull origin main
   ```

3. **Пересоберите Docker образ:**
   ```bash
   docker-compose build --no-cache
   ```

4. **Запустите бота:**
   ```bash
   ./scripts/start.sh
   ```

5. **Проверьте статус:**
   ```bash
   ./scripts/status.sh
   ```

## 📊 Мониторинг

### Новый скрипт мониторинга:
```bash
# Однократная проверка
./scripts/monitor.sh

# Автоматический перезапуск при проблемах
./scripts/monitor.sh auto-restart

# Непрерывный мониторинг
./scripts/monitor.sh watch
```

### Проверка логов на ошибки:
```bash
./scripts/logs.sh | grep -i "pool timeout\|error\|exception"
```

### Health check:
```bash
curl http://localhost:8080/health
```

## 🔧 Дополнительные настройки

### Если проблемы продолжаются:

1. **Увеличьте CONNECTION_POOL_SIZE в .env:**
   ```env
   CONNECTION_POOL_SIZE=50
   ```

2. **Увеличьте таймауты:**
   ```env
   POOL_TIMEOUT=60.0
   READ_TIMEOUT=60.0
   WRITE_TIMEOUT=60.0
   ```

3. **Добавьте ограничение скорости запросов:**
   ```python
   # В bot.py можно добавить throttling
   from asyncio_throttle import Throttler
   throttler = Throttler(rate_limit=30, period=1)  # 30 запросов в секунду
   ```

## 🚨 Экстренные меры

### Если бот полностью завис:

1. **Принудительная остановка:**
   ```bash
   docker-compose down --remove-orphans
   docker system prune -f
   ```

2. **Полная пересборка:**
   ```bash
   docker-compose build --no-cache --pull
   docker-compose up -d
   ```

3. **Проверка ресурсов сервера:**
   ```bash
   free -h        # Память
   df -h          # Диск
   htop           # Процессы
   ```

## 📈 Оптимизация производительности

### Настройки Docker:
```yaml
# В docker-compose.yml
deploy:
  resources:
    limits:
      memory: 1G      # Увеличить если нужно
      cpus: '1.0'     # Увеличить если нужно
```

### Настройки системы:
```bash
# Увеличить лимиты файловых дескрипторов
echo "* soft nofile 65536" >> /etc/security/limits.conf
echo "* hard nofile 65536" >> /etc/security/limits.conf
```

## 🔍 Диагностика

### Полезные команды:
```bash
# Статистика Docker контейнера
docker stats telegram-bot-analiz-ca-bot

# Подключения к Telegram API
netstat -an | grep :443

# Логи с фильтрацией
docker logs telegram-bot-analiz-ca-bot 2>&1 | grep -E "(ERROR|Pool|timeout)"

# Проверка DNS
nslookup api.telegram.org
```

## 📞 Контакты для поддержки

Если проблемы продолжаются:
1. Сохраните логи: `./scripts/logs.sh > bot_logs.txt`
2. Сохраните статус: `./scripts/status.sh > bot_status.txt`
3. Проверьте мониторинг: `./scripts/monitor.sh > bot_monitor.txt`